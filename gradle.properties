# Force Gradle (and the compile toolchain) onto Java 11.
# Voldeloom 2.4 / MC 1.6.4 need Java 11; your default `java` is 26, which Gradle 7.5.1 can't run on.
# Forward slashes on purpose — backslashes are escape characters in .properties files.
org.gradle.java.home=C:/Users/Josh/scoop/apps/temurin11-jdk/current
